from abc import ABC, abstractmethod


class Cat:

    def __init__(self, age, saturation_level):
        self.age = age
        self.saturation_level = 50

    @abstractmethod
    def set_average_speed(self, average_speed):
        pass



    def _increase_saturation_level



if __name__ == "__main__":
    kote = Cat(23)
    if saturation_level < 0:
        print(0)
    elif saturation_level > 100:
        print(100)